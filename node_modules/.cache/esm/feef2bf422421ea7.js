let Drawlib,Color;_224‍.w("./drawlib.js",[["*",null,function(v){Drawlib=v}]]);_224‍.w("./color.js",[["*",null,function(v){Color=v}]]);


// Fonction pour dessiner un arbre
function drawTree(x, y, size) {
  const treeColor = Color.rgb(139, 69, 19); // Brown color
  const leafColor = Color.rgb(34, 139, 34); // Green color

  const trunk = Drawlib.rectangle(treeColor, 20, size * 2);
  const leaves = Drawlib.circle(leafColor, size);

  const tree = Drawlib.group([
    Drawlib.move(x, y - size, trunk),
    Drawlib.move(x, y - size * 3, leaves)
  ]);

  Drawlib.renderCentered(tree, context);
}

// Fonction pour dessiner un mouton
function drawSheep(x, y) {
  const bodyColor = Color.rgb(255, 255, 255); // White color
  const headColor = Color.rgb(255, 220, 185); // Light brown color
  const legColor = Color.rgb(139, 69, 19); // Brown color

  const body = Drawlib.circle(bodyColor, 30);
  const head = Drawlib.circle(headColor, 20);
  const leg1 = Drawlib.rectangle(legColor, 10, 40);
  const leg2 = Drawlib.rectangle(legColor, 10, 40);

  const sheep = Drawlib.group([
    Drawlib.move(x, y, body),
    Drawlib.move(x - 30, y - 30, head),
    Drawlib.move(x - 15, y + 20, leg1),
    Drawlib.move(x + 15, y + 20, leg2)
  ]);

  Drawlib.renderCentered(sheep, context);
}

// Obtenez le contexte du canvas
const canvas = document.getElementById("canvas");
const context = canvas.getContext("2d");

// Dessinez plusieurs arbres et moutons
drawTree(100, 400, 40);
drawTree(300, 400, 60);
drawTree(500, 400, 50);

drawSheep(700, 500);
drawSheep(200, 500);
drawSheep(400, 500);
